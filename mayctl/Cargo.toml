[package]
name = "mayctl"
version = "0.1.0"
edition = "2021"

[dependencies]
mayland = { version = "0.1.0", path = ".." }
postcard = { version = "1.0.8", default-features = false, features = ["use-std"] }
